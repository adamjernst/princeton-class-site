README

You MUST use django 1.1 and django-cms 2.0.2, installed as follows:

# easy_install django==1.1
# easy_install django-cms==2.0.2

You can also install the other dependencies as follows:

# easy_install django-paypal
# easy_install django-reversion
